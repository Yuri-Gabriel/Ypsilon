{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": ["teste.y"],
            "cwd": "${workspaceFolder}"
        }
    ]
}